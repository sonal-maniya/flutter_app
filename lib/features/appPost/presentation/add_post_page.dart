import 'package:flutter/material.dart';

class AddPostPageWidget extends StatelessWidget {
  const AddPostPageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Add New Post"),
    );
  }
}
